global:
  metric_name_escaping_scheme: underscores

# No scrape config for oxide metrics — they are pushed via remote write.
# Only scrape the collector's internal metrics.
scrape_configs:
- job_name: otelcol-internal
  static_configs:
  - targets:
    - otelcol:8888
